<h1> Choose the times you want reality checks sent!<h1>
<%= form_for [@category, @timer], :url => category_timer_path(@timer), :method => :patch do |f| %>

    <%= f.label "update!"  %><br>
    <%= f.select :rate, [ 1, 5, 12 ], :action => :patch %>

    <%= f.submit %>
<% end %>
